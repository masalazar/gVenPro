<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cl.issoft.www.ismaq">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service android:name=".ConexionServidor" />
        <activity
            android:name=".ActMenu"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ActEditNota"
            android:label="Mantenedor de Nota"
            >
        </activity>
        <activity
            android:name=".ActEjeSQL"
            android:label="Ejecutar SQL"
            >
        </activity>
        <activity
            android:name=".ActEscFecha"
            android:label="Escoger Fecha"
            >
        </activity>
    </application>

</manifest>